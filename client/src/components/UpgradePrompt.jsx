import { useState } from 'react'
import { useSession } from '../lib/session.jsx'
import { TrophyIcon } from '../lib/icons.jsx'

// Upgrade prompt hidden until payments are ready.
export default function UpgradePrompt() {
  return null
}
